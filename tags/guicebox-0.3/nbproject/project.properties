application.title=GuiceBox
application.vendor=will
auxiliary.org-netbeans-modules-projectimport-eclipse-core.key=src=src;src=sample;src=test;ant=libs.Guice.classpath;ant=libs.EasyMock.classpath;ant=libs.JCIP_Annotations.classpath;jre=JDK 1.6;output=classes;
auxiliary.org-netbeans-modules-projectimport-eclipse-core.project=.
auxiliary.org-netbeans-modules-projectimport-eclipse-core.timestamp=1255383053000
auxiliary.org-netbeans-modules-projectimport-eclipse-core.workspace=..
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/GuiceBox.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.GuiceBox-sample=sample
file.reference.GuiceBox-src=src
file.reference.GuiceBox-test=test
includes=**
jar.compress=true
javac.classpath=\
    ${libs.Guice.classpath}:\
    ${libs.EasyMock.classpath}:\
    ${libs.JCIP_Annotations.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:fallthrough
javac.deprecation=true
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=true
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=true
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/Users/will/Development/Java/GuiceBox/GuiceBox/dist
jnlp.descriptor=application
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
main.class=sample.empty.EmptyFailover
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-ea
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
sample.dir=${file.reference.GuiceBox-sample}
source.encoding=UTF-8
src.dir=${file.reference.GuiceBox-src}
test.src.dir=${file.reference.GuiceBox-test}
